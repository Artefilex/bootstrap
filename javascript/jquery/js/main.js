// external js kullanılıyorsa document ready kullanmamız lazım 

$(document).ready(()=>{
    $('button').click(() => {
        // $("#jquery").hide()
        // $(".blabla").hide()
          $("p").hide()
       })
       
});

